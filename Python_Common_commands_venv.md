# to create virtual environment in python 
    python -m venv venv

# to activate virtual environment mac 
    source venv/bin/activate
for windows cmd 
    venv\Scripts\activate.bat

# to install a package 
    pip3 install openai --no-cache-dir -r requirements.chatgpt.txt
    pip install openai --no-cache-dir -r requirements.chatgpt.txt

    

